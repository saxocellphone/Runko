# CLI contract

The CLI (`runko`, `runko-ci`) is the **primary agent interface** (docs/design.md
§8.3, decided 2026-07-07) - MCP is a thin remote adapter over the same
backing logic for clients that can't shell out, not the primary surface.
This document is the contract a script or agent can rely on: exit codes,
`--json` output, and the error shape, so scripting against these CLIs
doesn't require reading source to guess behavior.

## Exit codes

| Code | Meaning |
|---|---|
| `0` | Success |
| `1` | The command was understood but failed - a structured error (see below) is printed to stderr |
| `2` | Usage error - unknown command, wrong/missing subcommand keyword, or unparseable flags |

Flag-parsing errors (bad flag names, malformed values) exit `2` with a
one-line error plus a `Run 'runko <cmd> --help' for usage.` pointer. A
recognized command with a missing *required* value (e.g. `project create`
without `--name`) exits `1`, not `2` - it parsed fine syntactically; the
failure is a validation error, not a usage error, matching how
required-field errors are reported everywhere else in this codebase
(§6.5's `required_field` code).

## Command tree, help, and flag conventions (clig.dev redesign, 2026-07-22)

Both CLIs are a cobra/pflag command tree; the shell of every command -
help, completions, flag parsing - follows [clig.dev](https://clig.dev),
while every command name, alias, `--json` shape, and exit code above is
unchanged from the pre-cobra CLI. What scripts can rely on:

- **POSIX flags.** Long flags take `--flag` (double dash); the short
  forms are `-m` (`--message`), `-w` (`--workspace`), `-h` (`--help`),
  and root-level `-v` (`--version`, composing with `--json`). The old
  stdlib-flag tolerance for single-dash long forms (`-json`) is gone -
  every documented invocation has always used `--`. Flags and
  positionals intersperse: both `runko change resolve <id> --undo` and
  `... --undo <id>` parse. A stray positional on a flags-only command
  is refused as a usage error (exit `2`) - the pre-cobra CLI silently
  ignored trailing arguments.
- **Global connection flags.** `--runkod-url` and `--token` are root
  persistent flags, accepted anywhere on the line. Credentials resolve
  flags > `RUNKO_RUNKOD_URL`/`RUNKO_TOKEN` env > the stored login - the
  env fallback that used to be `agent event`-local is now uniform.
  `RUNKO_TOKEN` takes either a bare bearer token or the name-qualified
  `<name>:<token>` form `agent create` prints (authenticates as that
  principal, HTTP Basic - the same split the git push transport already
  applied); with `RUNKO_TOKEN` set and no URL from flag or env, the
  stored login supplies the control plane (an exported token retargets
  WHO authenticates, not WHERE - `missing_url` only when there is no
  stored login either).
- **Help.** `-h`/`--help`/`help <cmd>` print full help to stdout, exit
  `0`. A noun command run bare (`runko change`) prints its help to
  stderr and exits `2` (the historical usage-error contract); an unknown
  keyword adds did-you-mean suggestions before exiting `2`.
- **Completions.** `runko completion bash|zsh|fish|powershell` emits the
  shell's completion script (cobra-generated; `runko-ci` likewise).

## Structured errors (§6.5)

Every error this session's CLI code can identify a specific cause for -
"not a git repository", "detached HEAD", "unresolvable revision", "no
commits yet" - is surfaced via `internal/clierr.Error`:

```text
<message>
  -> <suggestion>
  see <doc_url>
```

printed to stderr, exit code `1`. This replaces git's raw `exit status 128`
passthrough for those specific, identifiable cases. Errors this session's
code has no more specific classification for (a failed `git clone` against
an unreachable remote, a genuine merge conflict) still surface git's own
message text - that IS the most useful information available for those
cases, not a gap to paper over with a generic wrapper.

## `--json` output

Every subcommand that produces data (not just a side-effect confirmation)
supports `--json`, emitting one JSON object to stdout instead of a human
summary line. Field names are the Go struct's exported field names
(capitalized) unless otherwise noted - this codebase does not yet generate
these shapes from `docs/spec/`, so treat field names as this table
documents them, not as guaranteed-stable API in the way `docs/spec/`
schemas are.

| Command | `--json` output |
|---|---|
| `runko project create` | `{"name", "path", "rev", "change_id"}` - the create commit is born with its Change-Id trailer (same minting as `change create`; entropy-seeded, so two clones creating the same name never collide on identity), so the scaffold is pushable as-is with no amend step. `--lang` selects the built-in template language (`go` default, `python`, `ts`, `rust`, `java`, `cpp` — the §10.4 Bazel-maturity set); any other well-formed language needs `--no-template` (`unsupported_language` otherwise), which scaffolds only PROJECT.yaml + README and records the language verbatim. `--build-engine bazel\|vite\|none` selects the build scaffold, defaulting by language (`ts` → vite, else bazel; §14.5.5) — vite emits package.json + vite.config.ts with no `build` capability, unknown engines are `unsupported_build_engine`. `--api grpc\|rest\|none` decides the contract surface at creation (§13.3.1) — REQUIRED for `--type service` (`api_required` otherwise), optional for `--type app`, and `grpc`/`rest` on `library`/`job`/`other` is `invalid_combination` (serving types only); `grpc` scaffolds the `rpc` capability + an in-boundary proto stub (`<path>/proto/<name>/v1/<name>.proto` + `buf.gen.yaml`), `rest` scaffolds the `http` capability + a minimal in-boundary `openapi.yaml`, unknown values are `unsupported_api` |
| `runko doctor` | `DoctorReport` (`RepoDir`, `TrunkRef`, `HasRemote`, `RemoteName`, `RemoteURL`, `HasChangeIDHook`, `HasVerbNudgeHook`, `HooksDir`, `GitVersion`, `GitVersionOK`, `GitVersionError`, `IsJJWorkspace`, `JJChangeIDsWired`, `WorkspaceID`, `HasAgentHooks`, `TrackedMaterializations`, `CLI` (this binary's `BuildIdentity`, printed first in the cheat-sheet - see `runko version`) - workspace binding + §12.6.1 activity hooks (reported only inside a workspace), and the §12.7 registry rows whose directories survive on this machine (the cheat-sheet points at `workspace gc` when > 0)). `--install-hook` installs the commit-msg Change-Id hook AND the advisory pre-commit verb nudge (answers a raw `git commit` with the native verbs on stderr, jj-aware, never blocks; a foreign pre-commit hook is left alone with a note). In a jj workspace it also sets `templates.commit_trailers` so Change-Id trailers derive from jj change ids (refuses to clobber a foreign trailers template: `jj_trailers_conflict`) |
| `runko status` | `StatusReport` (`Dir`, `IsJJWorkspace`, `WorkspaceID`/`Branch` (the `runko.workspace`/`runko.branch` binding; `Branch` defaults `head`, both empty outside a workspace checkout), `WorkspaceStatus` (the server's row, `""` when unknown), `Remote`, `TrunkRef`, `DirtyPaths` (`git status --porcelain -uall` count - what the next `change create` sweeps in), `StaleBase` (the remote trunk tip is missing from this line's ancestry; best-effort - an unreachable remote reads not-stale), `Principal`/`ControlPlane` (live `whoami`), `ServerError` (why server facts are missing; `""` when live), `TrunkSHA`/`TrunkTitle` (the local remote-tracking trunk ref the stack diffs against - the graph's `◆` base; may lag the real remote tip, which is what `StaleBase` reports), `Stack` (`[]StackEntry {ChangeID, SHA, Title, Status, Blockers}`, bottom -> top above the local `refs/remotes/<remote>/<trunk>`; `null` when that ref doesn't exist locally, `[]` when nothing is in flight)) - the orientation verb (2026-07-23): one look answers workspace binding, identity, base freshness, working-tree dirt, and each stacked change's gates. HUMAN mode draws the line above trunk the way `jj log` does (2026-07-23): newest on top, `@` where the working copy sits (the uncommitted working tree when one exists, else the tip change), `○` each change under it with its gates in parentheses, `◆` the trunk base (`TrunkSHA`/`TrunkTitle`), and a node's blockers on its `│` gutter; an empty `Stack` prints one line (`stack: nothing in flight - HEAD is on trunk`) instead of an empty graph - dirt stays on the working-tree line only; `--json` keeps `Stack: []` (shape stable). Per-entry `Status`: `ready` (open + mergeable), `blocked` (open, + plain-language `Blockers`), `landed`/`abandoned` (the server's own state - landed entries mean the line still carries commits trunk already has, e.g. a stale local trunk ref), `not_pushed` (the control plane has no such change), `not_a_change` (no Change-Id trailer yet - a described jj working-copy commit, or a raw git commit in an unhooked checkout; the graph node shows the short SHA, `(no description set)` for an empty subject, and the stamp-it hint), `unknown` (no credential/unreachable server). LOCAL facts never require a network: with no credential or an unreachable control plane the command still exits 0 with `ServerError` named - jj-aware (the stack tip is jj's working copy; an undescribed `@` with no Change-Id is dropped from `Stack` entirely - empty or dirty - because its content is already on the working-tree line; a described `@` stays as genuine WIP). Structured refusal: `not_a_repo` |
| `runko project delete` | `{"change_id", "title"}` - create's dual (§13.1, decided 2026-07-15): calls the server intent API (needs a live runkod + credential; deletion needs the trunk-tip index for edge-stripping, and a sparse worktree may not hold the files), which authors an ordinary open Change deleting the project subtree and stripping the name from every other manifest's `dependencies:`/`consumes:`. Refusals are structured: `unknown_project`, `root_project_immutable`, `agents_cannot_delete_projects` |
| `runko project list` | `[]index.IndexedProject` (`Name`, `Path`, `Type`, `Capabilities`, `DeclaredDependencies`, `Consumes`, `ContractDir`/`ContractGenDir` + `OpenAPIPath`/`OpenAPIPresent` (§13.3.1 contract surface; zero-valued without the rpc/http capabilities), `Visibility`, `Owners` `[{Ref, Source}]`, `RequiredChecks`) - needs a live runkod (`GET /api/projects`, the trunk-tip project index per §10.3), see §28.3 stage 12. HUMAN output only (2026-07-21): the repo-ROOT project sorts first and renders its (empty) path column as `(root)` - it owns every path no deeper manifest claims and carries the repo-wide `root_invalidation`/`prose` rules, so it is not a peer of the rest. Rootness is the PATH (`""` or `"."` - the daemon's own rule), never the name. `--json` is byte-unchanged |
| `runko change push` | `{"change_id", "ref"}`. Refuses when the tip is already on the remote trunk (`already_on_trunk`). Auto-syncs first (2026-07-09): a base missing the remote trunk tip is rebased onto it before the push (jj-aware); `--no-sync` pushes as-is. A CONFLICTING sync does not block the submit (2026-07-17): the checkout is restored to its pre-sync state (plain git aborts the rebase; jj rolls back to the pre-sync operation - conflict markers never reach the pushed commit), a stderr warning names the files, and the stale base is pushed - conflicts gate LANDING (the land engine refuses a conflicting rebase, §13.5), not review or CI. In a jj workspace (colocated; `.jj` at the repo top level): the tip comes from jj's working copy (an empty, undescribed `@` is skipped in favor of `@-`), a missing trailer is a structured error (`jj_change_ids_not_configured` - the CLI never amends behind jj's back), and the push is by commit SHA since git HEAD is detached in colocated repos. One push updates EVERY Change in the pushed stack (series receive, §7.4) - after reworking the root of a stack, jj auto-rebases descendants and a single `change push` restacks the server. Changes are born in workspaces (§12.2, 2026-07-09): the server refuses a push with no workspace origin by default - push from a `runko workspace attach`ed worktree (origin stamped automatically) or, with plain git, `git push -o workspace=<id>`; a brand-new monorepo's bootstrap push is exempt. A re-push whose new head is a TRIVIAL REBASE of the previous head (auto-sync's normal product: same message, delta replays cleanly) carries owner approvals - and, under `revalidation: conflict-only`, passing check results - to the new head; when every required check is covered the push triggers no CI re-run (§13.5 carry-forward, 2026-07-15). Content/message amends and conflict-resolved rebases reset everything as before; a same-head re-push still re-triggers CI |
| `runko change land` | `land.Outcome` (`Landed`, `LandedSHA`, `RequiresRevalidation`, `Conflicts`, `RaceRetry`) - needs a live runkod (`--runkod-url`/`--token`), unlike every other command in this table, see §13.5/§28.3 stage 11b. `--change` defaults to HEAD's `Change-Id` trailer (inside a checkout; stderr notes the resolved id); pass `--change` explicitly outside one. `--force` is the §13.5 admin override: bypasses owner/check gates and revalidation (server-authorized - admin principals and the deploy token only, 403 `force_denied` otherwise; never bypasses conflicts, stacked-parent ordering, or terminal states), audited durably as `landed_forced`. Under the default `revalidation: conflict-only` policy (§13.5, 2026-07-15) a green change whose rebase is clean lands with zero re-runs and the recovery loop never engages; `requires_revalidation` arises only under org-tightened `affected-intersection`/`always`. By default (`--sync`, needs a checkout at `--repo`) a `requires_revalidation` 409 runs the §13.5 recovery loop client-side: sync onto trunk, re-push, poll the gates, retry - bounded by 4 attempts and `--sync-timeout` (15m); failed checks after a sync stop the loop with `checks_failed`. `--no`-op without a local checkout, and never engaged with `--force` |
| `runko change approve` | `MergeRequirements` (the same nested `{change_id, owners, checks, mergeable, blockers}` shape `GET .../merge-requirements` reports, per `docs/spec/mcp-tools/common.schema.json`) - needs a live runkod, see §13.5/§28.3 stage 11c |
| `runko change ack-policy` | `checks.MergeRequirements` (the refreshed gates; human mode prints acknowledged + mergeable/blockers) - the 2026-07-24 enforcement split's "extra button": completes the RESERVED `agent-policy` check an agent change's accepted-but-flagged policy findings minted (denylisted paths, size caps, owners edits - warned at push time). Approve-rights humans only: agents are `agent_ack_denied`, a named principal acks as itself (`acked_by_mismatch` otherwise), `--by` covers the anonymous-deploy-token form. `nothing_to_acknowledge` when the head carries no findings; `already_acknowledged` (409) on a double ack; an amend re-evaluates policy and resets the acknowledgement. The check name is refused from `report-check` (`reserved_check_name`) and unknown to `rerun-check` |
| `runko change list` | `[]ChangeInfo` (`ChangeKey`, `State`, `BaseSHA`, `HeadSHA`, `GitRef`, `Title`, `Description`, `TestPlan`, `LandedSHA`, `AuthoredBy`, `LandedBy`) - needs a live runkod (`GET /api/changes?state=`; `--state all` lists every state), see §28.3 stage 12c |
| `runko change abandon` | `ChangeInfo` - needs a live runkod; idempotent on an abandoned change, refuses a landed one (`invalid_state`), see §7.4/§28.3 stage 12c |
| `runko change describe` | `ChangeInfo` - sets the §8.6 summary on an open change (`POST /api/changes/{key}/describe`; default `--change`: HEAD's Change-Id trailer): `--description` says what the change does and why, `--test-plan` how it was verified - agents SHOULD set both after push; shown on the change page and the input to §14.10.3 release changelogs. An omitted flag preserves the stored value, an explicit `""` clears it; unlike `Title`, neither field moves on amend. Only open changes take edits (`invalid_state` otherwise - a landed change's description is part of the record its release changelog derives from) - needs a live runkod |
| `runko change rerun-check` | `MergeRequirements` (refreshed, same as approve) - needs a live runkod; resets a REQUIRED check to queued and emits `change.check_rerun_requested` for the org's CI plugin (§14.4.2), see §28.3 stage 12c |
| `runko agents-md` | `{"path","skill_path","skill_paths"}` - writes `AGENTS.md` (default; `--out` overrides) at the repo root AND every generated agent skill (fixed paths, relative to `--repo`), the set org genesis seeds (§6.10), see §8.8/§28.3 stage 11. Two skills, split by job (2026-07-22): `.claude/skills/runko/SKILL.md` is the command reference (inventory, exit codes, error shape) and `.claude/skills/runko-workspaces/SKILL.md` is the workspace/change/land discipline - one unambiguous load trigger each, and the discipline one is 100% repo-agnostic so it scaffolds into any Runko monorepo. `skill_path` remains the reference skill for single-skill callers; `skill_paths` lists all of them |
| `runko ci init` | `ciInitResult` (`written[]`, `images`, `next_steps[]`) - LOCAL file-writer (no runkod call), like `agents-md`: copies the generic CI/CD GitHub Actions workflows from the embedded `templates/ci` into `<dir>/.github/workflows/` - `runko-checks.yml` (pre-land CI, `runko-change` dispatch) always, `runko-images.yml` (post-land CD, `runko-image-build`) additionally with `--images`. The workflows download the `runko-ci` binary themselves and read every project/check/image/registry fact from the tree, so this command names none (§14.9.1). Refuses to overwrite an existing workflow without `--force` (`workflow_exists`, pre-checked so a partial scaffold never half-writes); `--executor` accepts only `github` today (`unsupported_executor`). `next_steps` names the tree+org wiring it deliberately leaves to you: `ci.checks`, the RUNNER CONTRACT, App-install-then-`github connect`, and the `RUNKO_URL`/`RUNKO_CI_TOKEN` secrets |
| `runko workspace create` | `WorkspaceInfo` (`ID`, `Owner`, `BaseRevision`, `ProjectAffinity`, `WriteAllowlist`, `SnapshotRef`, `Status`, `SparsePatterns`, `RepoPath`, `TrunkRef`) + `Dir` (where the worktree landed - callers may have passed no `--dir`) - needs a live runkod, see §12.3/§28.3 stage 12b. Placement is §12.7: with no `--dir`/`--clone-dir` the worktree and shared store land under `$RUNKO_WORKSPACE_HOME` (default `~/runko-ws`) in a `<host>/<org>/<repo>/{.store,<workspace>}` layout; materializing inside another git checkout is refused (`workspace_nested_checkout`, pre-flighted for explicit paths BEFORE the registration POST; `--force-nested` overrides). The store is credential-neutral: its remote URL never embeds a secret - raw git asks the stamped `runko auth git-credential` helper, runko's own verbs inject per-invocation auth; a pre-§12.7 store is neutralized on the next create/attach through it. Create also runs a bounded auto-gc on the store and prefers RECYCLING a reclaimable worktree (rebind: move + cone rewrite + `checkout -B` + `clean -fd` without `-x`, so ignored caches survive; a `recycled <path>` note goes to stderr). `--by` defaults to the stored login's principal (§6.10) - only a bare deploy token still needs the flag. `--new-path <dir>` (repeatable, 2026-07-16 - the greenfield bootstrap) grants the workspace affinity + cone coverage for a project that does NOT exist at trunk yet, so the change that creates it has a workspace to be born in; legal with zero `--project` flags, joins `WriteAllowlist`/`SparsePatterns` exactly like a resolved project path. The repo root (`--new-path .`/`` → the `""` path root) is a SPECIAL case (gap #4, 2026-07-22): allowed ONLY in a ROOT-LESS org (no project at the root), granting the same `""` root affinity `--project <root>` yields once one exists — the bootstrap for a fresh/mirror-adopted org's root files (`.github/`, a root `PROJECT.yaml`); if a root project already exists it is refused (`invalid_new_path`) pointing at `--project <name>`. Other refusals: `invalid_new_path` (unclean/absolute/`..`, incl. `--new-path /`), `project_exists_at_path` (already indexed - use `--project <name>`). Materialization installs BOTH client hooks into the shared store (the §6.9 verb nudge and the Change-Id commit-msg hook; an existing commit-msg hook is never overwritten). Human mode ends with the §12.6 streaming next-commands (`workspace watch`, `agent hooks --install`) plus §6.9's three-command loop (§6.10); never in `--json`. `--jj` (2026-07-16, §12.7) materializes a STANDALONE jj colocated checkout instead of a worktree off the store: full clone (jj cannot colocate inside a git worktree or lazy-fetch promisor blobs; `--clone-dir` is refused, `jj_no_shared_store`), `jj git init --colocate`, the §7.4 Change-Id trailer template, the cone mirrored via `jj sparse` (repo-root files enumerated for cone-mode parity), @ parked on base via a `ws/<id>/head` anchor branch, `runko.*` bound in plain config scope; needs the jj binary (`jj_not_found`) |
| `runko workspace list` | `[]WorkspaceInfo` - needs a live runkod. The human table ends with a `local:` column when this machine's §12.7 registry has surviving materializations for a row (first path, `(+n)` for more) |
| `runko workspace attach` | `WorkspaceInfo` + `Dir` - needs a live runkod. §12.7 placement/neutrality/auto-gc as on create (branch attaches default to `<workspace>@<branch>` under the managed home; attach never recycles - restore-from-snapshot is already the cheap path). Human mode ends with the §12.6 streaming next-commands; never in `--json`. `--jj` restores as a standalone jj colocated checkout (as on create; @ parks on the branch's snapshot tip) |
| `runko workspace path` | the materialization directory on stdout (`{"workspace","branch","path"}` with `--json`) - the escape hatch for what `-w` cannot cover: editing files, or a verb that takes only `--repo`. Not a `cd` helper: prefer `-w` on every verb that accepts it. No name: the current checkout answers for itself (`not_a_workspace` outside one); with a name (`<name[@branch]>` - `@branch` narrows to that branch's row): the registry's freshest surviving row (`not_materialized` when none) - local only |
| `runko workspace gc` | `[]GCCandidate` (`Materialization` fields + `Reclaim`, `Reason`, `SizeBytes`) - §12.7 reclamation. Plan-only by default (human mode prints reclaim/keep + sizes + reasons and a summary); `--apply` removes each reclaimable worktree, prunes its store's worktree metadata, and drops the registry row. Reclaimable ⇔ server-closed ∧ working tree == the snapshot ref's tree ∧ HEAD under the snapshot ref (watch-parked dirty worktrees evaluate correctly; a never-snapshotted worktree must be clean at its base) - everything doubtful is a fail-closed keep with the reason named, including an unreachable server. `--idle <dur>` extends to open-but-synced workspaces; `--scan <store>` (repeatable) first adopts a store's runko-bound worktrees into the registry (the pre-§12.7 migration path). Needs a live runkod |
| `runko change automerge` | `{"ChangeKey", "Automerge", "AutomergeBy", ...}` - arm the when-ready land (§13.5): the server lands the change automatically once checks + approvals go green, attributed to the armer; survives amends (gates reset and re-gate). `--change` defaults to HEAD's `Change-Id` trailer (inside a checkout; stderr notes the resolved id); pass `--change` explicitly outside one. `--disable` disarms. Only open changes arm (`invalid_state` otherwise) - needs a live runkod |
| `runko agent create` | `AgentIdentity` incl. `token` - shown exactly ONCE; mints an ephemeral task identity (`agent-<task>-<suffix>`, default TTL 8h, cap 168h). An agent credential is refused (`agents_cannot_mint`) - the harness or a human mints. Human mode ends with the §12.6 streaming next-commands incl. the exact env exports; never in `--json`. Needs a live runkod |
| `runko agent list` | `[]AgentIdentity` (no tokens) - live/expired/revoked task identities - needs a live runkod |
| `runko agent revoke` | `{"revoked"}` - immediate credential kill; the row survives for attribution - needs a live runkod |
| `runko agent event` | `{"recorded"}` - reports one activity event (kind `read\|edit\|command\|search\|note` + detail) to the workspace's §12.6.1 live feed; `--from-hook` derives kind/detail/session from a post-tool-use hook JSON on stdin; workspace from `runko.workspace` git config; credentials fall back to `RUNKO_RUNKOD_URL`/`RUNKO_TOKEN` env (flags > env > stored login - since 2026-07-22 the uniform rule for every control-plane verb, not just this one) - needs a live runkod |
| `runko agent hooks` | the ready-to-paste harness hooks JSON snippet on stdout (prerequisites on stderr); wires every post-tool-use call to `runko agent event --from-hook \|\| true`. `--install [--dir . | -w <ws>]` merges the snippet into the named (or current) workspace worktree's Claude Code `.claude/settings.local.json` instead (2026-07-14, opt-in only): foreign keys survive, an already-wired file no-ops, an unparseable one is refused (`invalid_settings`), and the file is excluded from snapshots (repo gitignore when it already covers it, else the shared clone's `info/exclude`); prints whether credentials resolve and the exact exports when they don't; `--json` (with `--install`) emits `{"path","installed"}`. Outside a workspace worktree and with no `-w`: `not_a_workspace` (the suggestion names `-w`) - local only |
| `runko workspace delete` | `{"deleted"}` - removes the registry row and every `refs/workspaces/<id>/*` snapshot ref; refused while the workspace has OPEN changes (`workspace_has_open_changes` - land or abandon first) and owner-only for named principals (`not_workspace_owner`, operators exempt). Local checkouts are never touched - needs a live runkod |
| `runko workspace snapshot` | `{"ref"}` - local git only (pushes to the worktree's workspace branch ref, `refs/workspaces/<id>/<branch>`; `head` is the default). In a bound jj colocated checkout the snapshot is built OUT-OF-BAND (the watch mechanics: throwaway index + commit-tree on HEAD) - never a commit on the checked-out line, which would rewrite history behind jj's back |
| `runko workspace watch` | NDJSON with `--json`: one `{"ref","sha"}` line per pushed snapshot (nothing on quiet ticks). The §12.6 auto-snapshot loop: every `--interval` (default 15s) it builds the working tree's snapshot OUT-OF-BAND (temp index + `commit-tree` - HEAD, the real index, and the worktree are never touched, safe beside a working agent or jj) and force-pushes when the tree moved. Policy rejections (secret scan, size cap) warn once per failure state and keep watching; terminal states (unbound dir `not_a_workspace`, closed/unregistered workspace) exit 1. `--once` does a single tick (always pushes unless the exact tree is already up) and exits - the CI/test form |
| `runko auth signup` | first contact, CLI-first (§6.10): wraps the hub's `POST /api/signup` (`--runkod-url` is the HOST root, not an `/o/<org>` mount) and on 201 stores the credential ALREADY pointed at the created/joined org's mount - signup IS login. Needs `--name`, `--org`, and exactly one of `--create`/`--join` (else structured `missing_field`); `--password` prompts hidden when omitted; `--invite-code` passes the `--signup-code` gate; `--email` is OPTIONAL (2026-07-20, migration 0022) and never prompts - omitted stores no address, a given one must be a bare `you@example.com` (`invalid_email`; no display-name form, nothing carrying CR/LF). Server refusal shapes relay verbatim (`signup_disabled`, `bad_signup_code`, `invalid_name`, `weak_password`, `invalid_email`, `name_taken`, `org_exists`, `unknown_org`, `org_create_disabled`, ... - §15.1's gates); a refused signup stores nothing. Human output names the next steps (`workspace create --project repo` against a created org's genesis-seeded root) |
| `runko auth login` | prints where the credential was stored; validates via `GET /api/whoami` first. Stores `{url, name?, secret}` at `$XDG_CONFIG_HOME/runko/credentials.json` when `XDG_CONFIG_HOME` is set (any platform), else the platform config dir (`~/.config/runko/` on Linux, `~/Library/Application Support/runko/` on macOS), 0600: with `--name` the secret is a principal password (sent as HTTP Basic - required for signed-up principals, whose passwords are hashed server-side), without it a bearer token. Every command taking `--runkod-url`/`--token` falls back to this stored credential |
| `runko auth status` | who the stored credential resolves to (live `whoami` round-trip) |
| `runko auth logout` | deletes the stored credential |
| `runko auth git-credential` | git's credential-helper protocol (called by git, not humans: `get`/`store`/`erase` on argv, attributes on stdin). Workspace stores stamp `credential.helper = !<runko> auth git-credential` (§12.7), so raw git in any worktree - including blobless lazy blob fetches - resolves the INVOKING principal's stored login; `get` answers username/password only for the stored login's own protocol+host (silence otherwise, so git falls through and a foreign remote is never fed runko's credential); `store`/`erase` are no-ops. `RUNKO_CREDENTIAL_HELPER` overrides the stamped command - local only |
| `runko change create` | `{"change_id"}` - local git only: commits ALL working-tree changes as one commit carrying a fresh `Change-Id` trailer (§7.4); no auto-push - `change push` stays the explicit submit step. Refuses newly-added files that look like build artifacts (executable+binary, or ≥5 MiB) with `suspect_artifact`, naming each - a stray `go build` output at the repo root no longer rides into the commit; `--allow-large` is the escape for an intentional binary asset |
| `runko change amend` | `{"change_id"}` - local git only: folds the working tree into HEAD's existing Change, PRESERVING its `Change-Id` (§7.4); `-m` rewords (default keeps HEAD's message). The native `git commit --amend`, carrying the §7.5 Runko-identity fallback so it works on a checkout with no configured git author. Refused in a jj colocated checkout (`jj_amend_unsupported` → `jj squash`/`jj describe`) |
| `runko change push` | `{"change_id","ref"}` - pushes HEAD (jj-aware) to `refs/for/<trunk>` with the worktree's workspace-origin push options; auto-syncs a stale base first (`--no-sync` opts out; a conflicting sync warns and pushes the stale base - resolve before landing, 2026-07-17) and, in a workspace-bound checkout, auto-snapshots the working tree beforehand (§12.6, best-effort - a snapshot failure warns and never blocks the push; `--no-snapshot` opts out). A workspace's first push that never streamed carries a one-time server advisory `remote:` block naming `workspace watch` + `agent hooks --install` (§12.6, 2026-07-14). A trivial-rebase re-push carries approvals (+ checks under `revalidation: conflict-only`) forward and, when fully covered, triggers no CI re-run (§13.5, 2026-07-15) |
| `runko change requirements` | `checks.MergeRequirements` - the §13.5 gates; `--change` defaults to HEAD's `Change-Id` trailer, needs a live runkod. Includes `attention_set` (§13.4.2): whose turn it is, derived from review requests + approvals + comments at the current head. Across every server verb that takes `--change`, a unique Change-Id prefix is accepted (resolved client-side against the change list; a full `I`+40hex id is used as-is with no extra call) |
| `runko change comment` | `CommentInfo` (`id`, `author {type, id}`, `body`, `created_at`, `path?`, `side?`, `line?`, `head_sha`, `parent_id?`, `resolved?` - the `ChangeComment` shape in `docs/spec/mcp-tools/common.schema.json`) - needs a live runkod. Anchors: change-level (no `--file`), file-level (`--file`), line-level (`--file --line`, `--side` defaults `head`); the server stamps `head_sha`, so an amend marks the comment outdated (§13.4.1). `--reply-to <root-id>` replies in a thread (one level deep - replying to a reply is `thread_depth_exceeded`; replies inherit the root's anchor). Agent principals comment with the agent badge; their approvals stay refused |
| `runko change comments` | `{"comments": [CommentInfo], "next_page_token"?}` (the `list_change_comments` MCP output shape) - needs a live runkod; the human view groups threads and marks `[resolved]`/`[outdated]`/`[agent]` |
| `runko change resolve` | `CommentInfo` (updated) - needs a live runkod; thread roots only (`not_a_thread_root`), allowed for the thread author, the change author, an owner of the anchored path, or an admin (`resolve_denied` otherwise); `--undo` reopens (§13.4.1) |
| `runko change request-review` | `{"reviewer"}` - needs a live runkod; records the request (idempotent), emits `change.review_requested`, and puts the reviewer in the derived attention set (§13.4.2) until they approve or comment at the current head |
| `runko workspace branch` | `{"ref"}` - local git only (forks a parallel line: switches this worktree's snapshot target to `refs/workspaces/<id>/<name>` and snapshots the fork point, §12.2). Refuses in a jj colocated checkout (`jj_checkout`): a parallel line there is a second standalone checkout (`runko workspace attach <id> --jj --branch <name>`) |
| `runko workspace sync` | `{"base_revision"}` - the CitC "sync to head" verb (§12.3): fetch trunk, rebase the workspace line onto its tip (jj-aware: in a colocated repo jj rebases so descendants follow), record the new base in the registry - needs a live runkod. Conflicts abort and name the files (`sync_conflict`). `update-base` is the original stage-12b alias |
| `runko org create` | `OrgInfo` (`name`, `role`, `api_base`, `git_url`, `default`) - needs a live runkod with `--allow-org-create`; the caller becomes the org's admin. Every org mounts the FULL surface (git, REST, RPC) under `/o/<org>/`, so pointing `--runkod-url` (or `runko auth login`) at `<host>/o/<org>` makes every other command in this table work against that org unchanged (§7.1). The new org is GENESIS-SEEDED (§6.10: root `repo` manifest, OWNERS naming the creator, AGENTS.md, CONTRIBUTING.md - trunk is never unborn), and the CLI REBINDS the stored login to the new org's mount (whoami-verified before saving; `--no-switch` keeps the old binding; the explicit `--token` scripting form stores nothing so nothing moves) - human output then names `workspace create --project repo` as the next step instead of a second `auth login`. Agents are refused (`agent_denied`, §8.7) |
| `runko org list` | `[]OrgInfo` - needs a live runkod. The shared default org (the root-mounted repo) is always first, marked `default`; store-backed accounts additionally see their memberships, operators see everything |
| `runko org add-member` | `{"org", "name", "role"}` - needs a live runkod; org admins and operators only (`not_org_admin` otherwise), and the account must already exist (`unknown_principal` - membership is not an invitation system). Non-members of an org get a structured 403 `not_org_member` (never 401) on every surface, git transport included. Roles: `member`, `admin`, `releaser` (§14.10.3: may write tags and cut releases when the org enforces tag policy) |
| `runko org bootstrap` | `{"seeded_genesis", "change_id", "title"}` - needs a live runkod; the §6.10 genesis RETROFIT for an org whose trunk resolves no owners anywhere (born before genesis, or imported bare - the default-deny deadlock where the OWNERS fix itself reads as unpoliced). Opens a server-authored Change adding root OWNERS naming the caller (plus the root `repo` manifest when none exists); owners resolve from the change's own head tree and a human owner-author self-satisfies by uploader consent, so the caller lands it immediately - `runko change land --change <id>` is printed as the next step. An unborn trunk is genesis-seeded directly instead (`seeded_genesis: true`, no change). Refusals: `already_governed` (409 - owners already resolve), `agents_cannot_bootstrap_org`, `bootstrap_needs_identity` (anonymous deploy token), `not_org_admin` (store-backed non-admins where org roles exist) |
| `runko org agent-policy` (`get` / `set` / `reset`) | `{"org", "overridden", "policy"}` for all three - OPERATOR ONLY, on the deployment admin surface (`/api/admin/orgs/{org}/agent-policy`, NOT the org mount; the CLI derives the deployment root from the credential URL, `--org` overrides). `policy` is the EFFECTIVE agent policy (§8.7): the stored operator override, or `DefaultAgentPolicy()` when `overridden:false` - the knobs governing an org's AGENTS (`denylist_paths`, `can_modify_owners`, `require_description`, size caps, `can_land_changes`, `can_enable_capabilities`). `get` reads; `set` is read-modify-write with convenience flags (`--allow-all-paths`, `--allow-workflows`, `--denylist`, `--allow-owners`/`--deny-owners`, `--can-land`/`--no-can-land`, `--max-files`, `--max-diff-bytes`) or `--from-json <file|->` for a COMPLETE policy (a stored row is a full policy - a future stricter field is a new column with a safe default, never a silently-loosened zero); `reset` drops the override. `set` WARNS (stderr) when it drops the `.github/workflows` denylist (a landed workflow reaches CI secrets) or enables can-land (composes with editable owners). A store account or an agent is refused (`operator_only`; `agents_cannot_set_policy` defends the surface). No org is loosened until `set` is run deliberately |
| `runko github connect` | `GithubConnectResult` (`org`, `repo`, `remote_url`, `mirror`) - needs a live runkod. One call wires the org to a GitHub repo (2026-07-16): the server verifies its deployment-wide GitHub App can actually push (a single ls-remote probes repo reachability, App installation, and installation-token minting), persists `github_mirror_repo` in org settings (boot-restored; the settings PUT never clobbers it), and arms the org's mirror worker live - no daemon restart, no `--org-mirror` edit. Boot flag config wins over stored wiring. Errors: `missing_field`/`invalid_repo` (400, `repo` must be `owner/name`), `github_app_not_configured` (412 - deploy runkod with `--github-app-id`/`--github-app-key-file`), `github_unreachable` (502 - repo missing or App not installed; the message names the fix), `force_denied` (403 - admins only, agents never). Since 2026-07-17 the wiring covers CI dispatch too (native Mode C: the outbox sends `repository_dispatch` itself - no per-org bridge); human output names the one remaining manual step, the mirror-side `runko-checks.yml` workflow |
| `runko github status` | `MirrorStatus` (`Configured`, `RemoteURL`, `Cursors[] {Ref, LastSyncedSHA, Frozen, UpdatedAt}`, `LastError?`, `LastSyncAt`) - needs a live runkod; GET /api/mirror/status rendered per ref with freeze state (`FROZEN` names the unfreeze endpoint). `Configured: false` suggests `runko github connect` |
| `runko release create` | `ReleaseInfo` (`project {id, name, path}`, `version`, `tag_ref`, `tag_sha`, `target_sha`, `head_change_key?`, `changelog?`, `created_by?`, `created_at` - the release block of `docs/spec/webhooks/release-created.schema.json`) - needs a live runkod. Cuts an IMMUTABLE release (§14.10.3): version explicit or auto patch-bump (`0.1.0` first; `missing_field` for manual-versioning projects without `--version`), changelog derived from landed changes touching the project since the previous release, annotated tag written server-side through the same tag policy raw pushes answer to (`release_denied` under enforcement without the releaser/admin role), `release.created` emitted. Errors: `release_not_enabled` (no `release` capability), `invalid_version`, `version_exists`, `tag_exists` - existing tags are never re-pointed |
| `runko release list` | `[]ReleaseInfo` - needs a live runkod; newest first. Releases have no edit or delete verb anywhere - a wrong release is followed by a corrected one |
| `runko self-update` | `UpdateOutcome` (`UpToDate`, `Updated`, `ReleaseCommit`, `Asset`, `Path`) - replaces the running binary with the rolling `cli-latest` GitHub release build (`.github/workflows/release-images.yml`'s `cli-release` job, the source of truth for binary distribution; §17.1) - needs GitHub, not runkod. Identity is content, not a version string: up to date iff the binary's sha256 matches the release's `checksums.txt` entry for this GOOS/GOARCH, so a from-source build reads as stale and is replaced. The download is verified against the published checksum before an atomic swap (temp file in the binary's own directory + rename; a symlinked install is resolved and its target replaced; Windows renames the old binary aside to `<path>.old`). `--check` reports without installing (exit 0 either way - the answer is the output, not the exit code); `--repo owner/name` retargets a fork's release; `update` is an accepted alias. Structured failures: `release_not_found`, `unsupported_platform`, `checksums_missing`, `checksum_mismatch`, `not_writable` |
| `runko version` | `BuildIdentity` (`revision?`, `time?`, `modified?`, `module?`, `go`) - which binary is this, from the Go toolchain's own VCS build stamp (no ldflags anywhere: checkout builds carry `vcs.revision`/`vcs.time`/`vcs.modified`, `go install m@v` builds carry the module version, an unstamped binary says `unstamped`). Human form is one line: `runko <rev12>[+dirty] (<time>, <goversion>)`. `-v`/`--version` are accepted aliases; `runko doctor` reprints the same identity as its first line, so any doctor paste answers "which runko bit us" (2026-07-16 dogfood review) |
| `runko-ci affected` | `affected.Result` (always JSON - no human mode exists for this command; it is CI-facing by design) |
| `runko-ci checks` | `{"run_everything", "checks": [{"project", "name", "command"}]}` (always JSON, like `affected`) - the §14.9 executor contract: the affected closure's manifest-declared `ci.checks`, deduped by name; the same name declared with different commands is a structured `ambiguous_check` error. `run_everything` resolves every project's checks (fail closed). `--post-land` (2026-07-24) additionally includes every project's `run_when: post_land` checks UNSCOPED - that class runs on every land, is never resolved without the flag, and is never required by the merge gate (`index.ChecksFor` excludes it), so only post-land CI passes it |
| `runko-ci images` | `{"run_everything", "images": [{"name", "image_ref", "context", "dockerfile", "build_args"?}]}` (always JSON, like `affected`/`checks`) - the §14.9.1 executor contract for the `deploy` capability (§14.10): which deployable images a `--base`/`--head` range must rebuild, each with its build config read from the OWNER project's `deploy.image`. `image_ref` is the full registry-qualified ref `<deploy_registry>/<name>` (the root manifest's `deploy_registry`; the bare `name` when unset) - the generic image-build workflow tags/pushes/reports it, hardcoding no registry. Resolved through the same `platform/deploy` derivation the daemon's deploy record uses (`openDeployRecordForLand`), so the built set and the record's expected set agree by construction. The same image name declared with different build config by two projects is a structured `ambiguous_image` error (identical declarations dedupe). Empty `images` for a non-deployable landing; `run_everything` rebuilds every declared image (fail closed). `gitops` (2026-07-24, optional) is the root manifest's `deploy_gitops` write-back target `{"repository", "kustomization"}` - the pin job's config source; absent when the root declares none, and a generic pin job skips on absence |
| `runko-ci binaries` | `{"run_everything", "releases": [{"tag", "binaries": [{"name", "path"}]}]}` (always JSON; 2026-07-24) - the standalone-release half of the `deploy` capability (docs/spec/deploy/README.md): the affected projects' `deploy.binaries` declarations grouped into rolling releases by tag. Republish rule is the declaring project's own affected membership (no rider edge; the dependency closure already answers "did anything it builds from change" - this retires the binary-release workflow's hand-maintained project list). The same binary name under one tag with different paths is a structured `ambiguous_binary` error (identical duplicates dedupe). Empty `releases` when no declaring project is affected; `run_everything` republishes every declared release (fail closed) |
| `runko-ci checkout` | `{"rev", "dest"}` |
| `runko-ci report-check` | `{"name", "status", "external_id"}` |

`runko mcp serve --runkod-url <url> --token <t>` is not in this table
because its stdout is not a `--json` data shape: it speaks newline-delimited
JSON-RPC 2.0 (the MCP stdio transport) until EOF, serving the seven read-only
`"status": "v1"` tools from `docs/spec/mcp-tools/catalog.json` as thin
wrappers over the same runkod REST API the commands above use (§8.3, §17.4,
§28.3 stage 12). Its tool outputs conform to `docs/spec/mcp-tools/`
schemas - the schema-of-record convergence the section below describes,
already real for this surface.

Commands not listed here (`runko auth` - stubbed, needs a live control
plane not built in this environment) have no output contract yet.

## Workspace-addressable checkout verbs (`-w`/`--workspace`, §12.7)

The worktree is transparent: every verb that operates on a local checkout
accepts `-w`/`--workspace <name[@branch]>` and resolves the workspace's
registered materialization (the §12.7 registry, the same lookup as
`workspace path`) as its working directory - so the verb runs from
anywhere, the repo root included, with no `cd` into the worktree. The
verbs: `status`, `change create`, `change amend`, `change push`,
`change land` (HEAD default + the `--sync` recovery checkout),
`change automerge`/`requirements`/`describe`/`comment`/
`comments`/`resolve`/`request-review` (the HEAD-default lookup),
`workspace snapshot`/`watch`/`branch`/`sync`, and `agent hooks --install`
(2026-07-22).

Deliberately absent: the server-side verbs that stay Change-Id-explicit
(`change approve`/`abandon`/`list` — abandon stays explicit because it is
destructive) have no checkout to resolve, so there is nothing for `-w` to
point at; `project create`, `doctor` and `agents-md` reach a checkout only
through `--repo`, for which `--repo "$(runko workspace path <ws>)"` is the
transparent form.

Resolution contract: `-w` absent falls through to `--dir`/`--repo` (the
historical stand-inside-it default, unchanged); `@branch` narrows to that
branch's materialization, else the workspace's freshest surviving row
wins; rows whose path is gone never match. Structured refusals: `-w` plus
an explicit `--dir`/`--repo` is `workspace_and_dir` (a contradiction -
pick one); a workspace with no local materialization is
`not_materialized`, suggesting `workspace attach` (with `--branch` when
`@branch` was asked) and `workspace list`, whose `local:` column shows
what is on this machine. A successful resolution refreshes the row's
`last_used_at` (the `workspace gc --idle` signal).

## Single-contract rule with MCP (§8.3)

Where the MCP tool catalog (seven read-only tools served by `runko mcp serve`
as of stage 12; the rest deferred to v1.x) and a CLI command cover the same
operation, they are meant to converge on the same wire shape -
`docs/spec/mcp-tools/` and `docs/spec/webhooks/` are the schema source both
should conform to. The CLI's hand-written JSON output above predates that
convergence for the commands that exist today; treat `docs/spec/` as the
schema of record once a given operation is generated from it, and this
table as the interim contract until then.

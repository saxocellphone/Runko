# Go
/bin/
*.test
*.out
vendor/

# Bazel (workspace-root convenience symlinks)
/bazel-*

# Vite dep cache that leaks out of web/ when tools run from the repo root
.vite/

# Local dev
.env
*.local.yaml

# Claude Code session state + agent worktrees (a second full checkout
# lives under .claude/worktrees/ - must never be committed or swept up
# by `git add -A` / `runko change create`)
.claude/

# OS
.DS_Store
